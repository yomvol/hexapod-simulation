---
- ros_topic_name: "/clock"
  gz_topic_name: "/clock"
  ros_type_name: "rosgraph_msgs/msg/Clock"
  gz_type_name: "gz.msgs.Clock"
  direction: GZ_TO_ROS

# /joint_states comes from the joint_state_broadcaster (via gz_ros2_control)
# and TF from robot_state_publisher — no other gz topics are bridged. The
# previously listed /world/.../joint_state and /model/robot/pose* entries
# belonged to the robot model block that is commented out of world.sdf.
